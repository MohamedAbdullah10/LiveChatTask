@page
@model LiveChatTask.Pages.Admin.IndexModel
@{
    ViewData["Title"] = "Admin Dashboard";
}

<div class="row mt-4 g-3">
    <!-- Active Users Sidebar -->
    <div class="col-12 col-lg-3">
        <div class="card h-100 shadow-sm">
            <div class="card-header bg-light">
                <h5 class="mb-0">Active Users</h5>
            </div>
            <div class="card-body p-0">
                <ul class="list-group list-group-flush">
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                        user1@livechat.com
                        <span class="badge bg-success rounded-pill">Online</span>
                    </li>
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                        user2@livechat.com
                        <span class="badge bg-success rounded-pill">Online</span>
                    </li>
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                        user3@livechat.com
                        <span class="badge bg-secondary rounded-pill">Idle</span>
                    </li>
                    <li class="list-group-item text-center text-muted small">
                        (Sample data - will be populated from live sessions)
                    </li>
                </ul>
            </div>
        </div>
    </div>

    <!-- Chat Panel -->
    <div class="col-12 col-lg-6">
        <div class="card h-100 shadow-sm d-flex flex-column">
            <div class="card-header d-flex justify-content-between align-items-center bg-primary text-white">
                <div>
                    <h5 class="mb-0">Chat with user1@livechat.com</h5>
                    <small class="text-white-50">Multiple conversations will be supported here</small>
                </div>
                <span class="badge bg-success">Live</span>
            </div>
            <div class="card-body d-flex flex-column p-0">
                <div class="p-3 border-bottom bg-light">
                    <span class="badge bg-secondary me-2">Sent</span>
                    <span class="badge bg-success">Seen</span>
                    <span class="text-muted small ms-2">Status flags displayed under each message</span>
                </div>

                <!-- Messages area -->
                <div class="flex-grow-1 p-3" style="overflow-y: auto; max-height: 420px;">
                    <!-- User message -->
                    <div class="d-flex mb-3">
                        <div class="bg-light border rounded-3 p-3">
                            <div class="small text-muted mb-1">User · 10:02</div>
                            <div>Hello, I need help with my subscription.</div>
                            <div class="small text-muted mt-1">
                                <span class="badge bg-secondary me-1">Sent</span>
                                <span class="badge bg-outline-secondary border">Text</span>
                            </div>
                        </div>
                    </div>

                    <!-- Admin message -->
                    <div class="d-flex justify-content-end mb-3">
                        <div class="bg-primary text-white rounded-3 p-3">
                            <div class="small text-white-50 mb-1">Admin · 10:03</div>
                            <div>Certainly. Can you confirm your account email?</div>
                            <div class="small text-white-50 mt-1">
                                <span class="badge bg-success me-1">Seen</span>
                                <span class="badge bg-outline-light border">Text</span>
                            </div>
                        </div>
                    </div>

                    <!-- Placeholder rich content message -->
                    <div class="d-flex mb-3">
                        <div class="bg-light border rounded-3 p-3">
                            <div class="small text-muted mb-1">User · 10:04</div>
                            <div>
                                <strong>Attachment:</strong> invoice-2026.pdf
                            </div>
                            <div class="small text-muted mt-1">
                                <span class="badge bg-secondary me-1">Sent</span>
                                <span class="badge bg-outline-secondary border">File</span>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Composer -->
                <div class="border-top p-3 bg-light">
                    <div class="mb-2 d-flex gap-2">
                        <button class="btn btn-outline-secondary btn-sm" type="button" disabled>
                            <span class="me-1">🖼</span> Image
                        </button>
                        <button class="btn btn-outline-secondary btn-sm" type="button" disabled>
                            <span class="me-1">📎</span> File
                        </button>
                        <button class="btn btn-outline-secondary btn-sm" type="button" disabled>
                            <span class="me-1">🎤</span> Voice
                        </button>
                    </div>
                    <div class="input-group">
                        <textarea class="form-control" rows="1" placeholder="Type a message..." disabled></textarea>
                        <button class="btn btn-primary" type="button" disabled>Send</button>
                    </div>
                    <div class="form-text small mt-1">
                        Message length and idle timeout rules are configured in the Settings panel.
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Settings Panel -->
    <div class="col-12 col-lg-3">
        <div class="card h-100 shadow-sm">
            <div class="card-header bg-light">
                <h5 class="mb-0">Chat Settings</h5>
            </div>
            <div class="card-body">
                <div class="mb-3">
                    <label class="form-label">Max characters per message</label>
                    <input type="number" class="form-control" value="500" min="50" max="2000" disabled />
                    <div class="form-text">Admin will be able to adjust this limit.</div>
                </div>

                <div class="mb-3">
                    <label class="form-label">Max idle minutes per user</label>
                    <input type="number" class="form-control" value="5" min="1" max="60" disabled />
                    <div class="form-text">If user is idle too long, an automatic warning will be sent.</div>
                </div>

                <div class="mb-3">
                    <label class="form-label">Idle warning message</label>
                    <textarea class="form-control" rows="3" disabled>
The chat will be terminated because we have not received a response from you.
                    </textarea>
                    <div class="form-text">This message will be auto-sent after configured idle time.</div>
                </div>

                <div class="alert alert-info small mb-0">
                    All controls are placeholders. In the next step, they will be wired to real-time chat logic.
                </div>
            </div>
        </div>
    </div>
</div>

